var x = 0;

eq (x, y) {
    var a = True;
    {
        var a = 1;
    }
    return x == y;
}

sum (x, y) {
    return x + y;
}

getX () {
    return x;
}


main() {
    var a = eq(1, 2);
    var b = eq('m', 'n');
    var c = eq(True : [], b : []);
}
